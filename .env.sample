# Environment variables for breast-segmentation project

# WandB API Key for experiment tracking
# Get your API key from: https://wandb.ai/authorize
WANDB_API_KEY=your_wandb_api_key_here

# CUDA device to use for training (default: "0")
# Set to specific GPU device number or "cpu" for CPU-only training
CUDA_VISIBLE_DEVICES=0

# Optional: WandB project name (defaults to "breast-segmentation" in code)
# WANDB_PROJECT=breast-segmentation

# Optional: WandB entity (your username or team name)
# WANDB_ENTITY=your_wandb_entity